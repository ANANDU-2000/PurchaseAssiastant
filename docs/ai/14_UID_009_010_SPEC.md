# Desktop UI Specs — UID-009 / UID-010 (slowness / refresh)

Check a box only when the diagnostic report confirms the issue **and** the diff fixes it.

Report: `docs/ai/15_diagnostic_report_slowness_refresh.md`

---

### UID-009: Foreground resume must not overlap refresh cycles
- [x] In-flight guard on `_onReturnedToForeground` so a second resume cannot start while one is running.
- [x] Throttle windows (2s / 30s) verified against live `DateTime.now()` (not a stale capture); `reloadHexaApp` kept or removed with explanation per diagnostic Step 3.

### UID-010: Long API wake / resume wait is legible; route TTL only if Scenario 4 confirms
- [x] Non-blocking reconnecting / waking indicator while long `pingHealth` runs on foreground resume (reuse `apiDegradedProvider` / section banners — not a full-screen blocker).
- [ ] Per-route freshness TTL (stock pattern) only for routes named in Scenario 4 — leave unchecked if Scenario 4 was not related.

---

## Why

- Overlapping resume + silent cold waits feel like freezes / thrash.
- Speculative route caching without Network proof is out of scope.
