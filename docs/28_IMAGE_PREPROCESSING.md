# Image Preprocessing (mobile)

## MVP
- Compress before upload (bandwidth + speed)
- Preserve readable contrast (avoid over-compress artifacts)

## Next (CamScanner-like)
- Crop + rotate before scan
- Optional contrast/clarity enhancement
- Multi-photo merge (long bills)

## Hard rule
- Never render raw bytes as text; always show the actual image preview.

