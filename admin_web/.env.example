# Leave empty for Vite dev proxy (recommended). Or set full API URL e.g. http://127.0.0.1:8000
VITE_API_BASE_URL=
# Super-admin JWT (paste after signing in via app bootstrap) — OR set the same value as backend ADMIN_API_TOKEN
VITE_ADMIN_BEARER=
